diff --git a/config.def.h b/config.def.h
index 6f05dce..d19e599 100644
--- a/config.def.h
+++ b/config.def.h
@@ -93,33 +93,39 @@ char *termname = "st-256color";
  */
 unsigned int tabspaces = 8;
 
-/* Terminal colors (16 first used in escape sequence) */
+/* Terminal colors (16 first used in escape sequence)
+ * Base16 Brush Trees Dark scheme by Abraham White &lt;abelincoln.white@gmail.com&gt;
+ */
 static const char *colorname[] = {
 	/* 8 normal colors */
-	"black",
-	"red3",
-	"green3",
-	"yellow3",
-	"blue2",
-	"magenta3",
-	"cyan3",
-	"gray90",
+	[0] = "#485867", /* base00 */
+	[1] = "#b38686", /* base08 */
+	[2] = "#87b386", /* base0B */
+	[3] = "#aab386", /* base0A */
+	[4] = "#868cb3", /* base0D */
+	[5] = "#b386b2", /* base0E */
+	[6] = "#86b3b3", /* base0C */
+	[7] = "#b0c5c8", /* base05 */
 
 	/* 8 bright colors */
-	"gray50",
-	"red",
-	"green",
-	"yellow",
-	"#5c5cff",
-	"magenta",
-	"cyan",
-	"white",
+	[8] = "#8299a1", /* base03 */
+	[9] = "#b38686", /* base08 */
+	[10] = "#87b386", /* base0B */
+	[11] = "#aab386", /* base0A */
+	[12] = "#868cb3", /* base0D */
+	[13] = "#b386b2", /* base0E */
+	[14] = "#86b3b3", /* base0C */
+	[15] = "#e3efef", /* base0F */
+
+	/* extra base16 colors */
+	[16] = "#d8bba2", /* base09 */
+	[17] = "#b39f9f", /* base0F */
+	[18] = "#5a6d7a", /* base01 */
+	[19] = "#6d828e", /* base02 */
+	[20] = "#98afb5", /* base04 */
+	[21] = "#c9dbdc", /* base06 */
 
 	[255] = 0,
-
-	/* more colors can be added after 255 to use with DefaultXX */
-	"#cccccc",
-	"#555555",
 };
 
 
diff --git a/x.c b/x.c
index 120e495..da3ec4b 100644
--- a/x.c
+++ b/x.c
@@ -754,7 +754,7 @@ xloadcolor(int i, const char *name, Color *ncolor)
 	XRenderColor color = { .alpha = 0xffff };
 
 	if (!name) {
-		if (BETWEEN(i, 16, 255)) { /* 256 color */
+		if (BETWEEN(i, 22, 255)) { /* 256 color */
 			if (i < 6*6*6+16) { /* same colors as xterm */
 				color.red   = sixd_to_16bit( ((i-16)/36)%6 );
 				color.green = sixd_to_16bit( ((i-16)/6) %6 );
