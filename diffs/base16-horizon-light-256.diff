diff --git a/config.def.h b/config.def.h
index 6f05dce..d19e599 100644
--- a/config.def.h
+++ b/config.def.h
@@ -93,33 +93,39 @@ char *termname = "st-256color";
  */
 unsigned int tabspaces = 8;
 
-/* Terminal colors (16 first used in escape sequence) */
+/* Terminal colors (16 first used in escape sequence)
+ * Base16 Horizon Light scheme by Michaël Ball (http://github.com/michael-ball/)
+ */
 static const char *colorname[] = {
 	/* 8 normal colors */
-	"black",
-	"red3",
-	"green3",
-	"yellow3",
-	"blue2",
-	"magenta3",
-	"cyan3",
-	"gray90",
+	[0] = "#fdf0ed", /* base00 */
+	[1] = "#f7939b", /* base08 */
+	[2] = "#94e1b0", /* base0B */
+	[3] = "#fbe0d9", /* base0A */
+	[4] = "#da103f", /* base0D */
+	[5] = "#1d8991", /* base0E */
+	[6] = "#dc3318", /* base0C */
+	[7] = "#403c3d", /* base05 */
 
 	/* 8 bright colors */
-	"gray50",
-	"red",
-	"green",
-	"yellow",
-	"#5c5cff",
-	"magenta",
-	"cyan",
-	"white",
+	[8] = "#bdb3b1", /* base03 */
+	[9] = "#f7939b", /* base08 */
+	[10] = "#94e1b0", /* base0B */
+	[11] = "#fbe0d9", /* base0A */
+	[12] = "#da103f", /* base0D */
+	[13] = "#1d8991", /* base0E */
+	[14] = "#dc3318", /* base0C */
+	[15] = "#201c1d", /* base0F */
+
+	/* extra base16 colors */
+	[16] = "#f6661e", /* base09 */
+	[17] = "#e58c92", /* base0F */
+	[18] = "#fadad1", /* base01 */
+	[19] = "#f9cbbe", /* base02 */
+	[20] = "#948c8a", /* base04 */
+	[21] = "#302c2d", /* base06 */
 
 	[255] = 0,
-
-	/* more colors can be added after 255 to use with DefaultXX */
-	"#cccccc",
-	"#555555",
 };
 
 
diff --git a/x.c b/x.c
index 120e495..da3ec4b 100644
--- a/x.c
+++ b/x.c
@@ -754,7 +754,7 @@ xloadcolor(int i, const char *name, Color *ncolor)
 	XRenderColor color = { .alpha = 0xffff };
 
 	if (!name) {
-		if (BETWEEN(i, 16, 255)) { /* 256 color */
+		if (BETWEEN(i, 22, 255)) { /* 256 color */
 			if (i < 6*6*6+16) { /* same colors as xterm */
 				color.red   = sixd_to_16bit( ((i-16)/36)%6 );
 				color.green = sixd_to_16bit( ((i-16)/6) %6 );
